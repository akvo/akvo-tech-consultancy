apiVersion: apps/v1
kind: Deployment
metadata:
  name: tech-consultancy-new-portfolio
spec:
  replicas: 1
  selector:
    matchLabels:
      run: tech-consultancy-new-portfolio
  template:
    metadata:
      labels:
        run: tech-consultancy-new-portfolio
        code-version: ${TRAVIS_COMMIT}
    spec:
      containers:
      - name: tech-consultancy-new-portfolio
        image: eu.gcr.io/akvo-lumen/tech-consultancy-new-portfolio:${TRAVIS_COMMIT}
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 100m
            memory: 200Mi
          limits:
            cpu: 1
            memory: 200Mi
---
apiVersion: v1
kind: Service
metadata:
  name: tech-consultancy-new-portfolio
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    run: tech-consultancy-new-portfolio
